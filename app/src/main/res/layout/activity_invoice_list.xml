<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:clipToPadding="false"
    tools:context=".ui.invoices.InvoiceListActivity">

    <!-- 1. TOOLBAR -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="40dp"
        android:paddingBottom="8dp"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:background="?attr/colorOnPrimary"
        android:elevation="4dp">

        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnVolver"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:text="@string/back_button"
                android:textColor="@android:color/holo_green_light"
                android:textAllCaps="false"
                android:textSize="20sp"
                app:icon="@drawable/ic_arrow_back_24"
                app:iconTint="@android:color/holo_green_light"
                app:iconGravity="textStart"
                app:iconPadding="0dp" />

            <TextView
                android:id="@+id/toolbarTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/facturas"
                android:textColor="@android:color/black"
                android:textSize="40sp"
                android:textStyle="bold"
                android:layout_marginTop="8dp"/>
        </LinearLayout>
    </androidx.appcompat.widget.Toolbar>

    <!-- 2. RECYCLERVIEW (Lista de Facturas) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/toolbar"
        android:padding="4dp"
        android:visibility="gone"/>

    <!-- 3. LOADING SKELETON (Shimmer) -->
    <com.facebook.shimmer.ShimmerFrameLayout
        android:id="@+id/shimmerViewContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/toolbar"
        android:visibility="visible">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">
            <!-- Items de ejemplo para el esqueleto -->
            <include layout="@layout/item_invoice_skeleton" />
            <include layout="@layout/item_invoice_skeleton" />
            <include layout="@layout/item_invoice_skeleton" />
            <include layout="@layout/item_invoice_skeleton" />
            <include layout="@layout/item_invoice_skeleton" />
            <include layout="@layout/item_invoice_skeleton" />
        </LinearLayout>
    </com.facebook.shimmer.ShimmerFrameLayout>

    <!-- 4. NUEVO EMPTY STATE / ERROR OFFLINE (Unificado) -->
    <!-- Usamos layout_centerInParent="true" porque estamos en RelativeLayout -->
    <LinearLayout
        android:id="@+id/layoutErrorState"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="gone"
        android:padding="32dp"
        android:layout_centerInParent="true">

        <ImageView
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:src="@drawable/ic_wifi_off_24"
            app:tint="#757575" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Sin conexión y sin datos"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@android:color/black"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="No se pudieron cargar las facturas."
            android:gravity="center"
            android:textColor="#757575"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnRetry"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:cornerRadius="20dp"
            android:layout_marginTop="16dp"
            android:text="Reintentar" />
    </LinearLayout>

    <!-- 5. EMPTY STATE (Sin resultados por filtros) -->
    <!-- Mantenemos este separado por si quieres distinguir "Error de red" de "Filtro sin resultados" -->
    <LinearLayout
        android:id="@+id/layoutEmptyState"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/toolbar"
        android:orientation="vertical"
        android:paddingBottom="200dp"
        android:gravity="center"
        android:visibility="gone"
        tools:visibility="gone">

        <ImageView
            android:layout_width="360dp"
            android:layout_height="360dp"
            android:src="@drawable/ic_empty_list"
            android:importantForAccessibility="no"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/sin_resultados"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#757575"
            android:layout_marginTop="16dp"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/change_filters"
            android:textSize="16sp"
            android:textColor="#9E9E9E"
            android:layout_marginTop="8dp"/>
    </LinearLayout>

    <!-- 6. FRAGMENT CONTAINER -->
    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:elevation="10dp"
        android:visibility="gone"
        android:background="@android:color/white"
        android:clickable="true"
        android:focusable="true"/>

</RelativeLayout>
